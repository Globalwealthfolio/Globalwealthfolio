---
import Layout from "../../layouts/Layout.astro";
---

<Layout
  title="Financial Freedom Is Not a Luxury — It's a Necessity to Truly Live — Global Wealth Portfolio Blog"
  description="Financial freedom is often framed as a privilege. But in a world of uncertainty, it is the foundation for a life lived on your own terms — and it is more accessible than you think."
>
  <article class="container-page page-section pt-3xl pb-2xl max-w-3xl">
    <a href="/blog" class="inline-flex items-center gap-xs text-body-sm text-link hover:underline mb-md">&larr; Back to blog</a>
    <div id="post-content">
      <p class="text-body-md text-body">Loading…</p>
    </div>
  </article>
</Layout>

<style is:global>
  .prose-custom h1,
  .prose-custom h2,
  .prose-custom h3 {
    color: var(--color-ink);
    font-weight: 600;
    line-height: 1.3;
    margin-top: 1.5em;
    margin-bottom: 0.5em;
  }
  .prose-custom h1 { font-size: 1.75rem; }
  .prose-custom h2 { font-size: 1.375rem; }
  .prose-custom h3 { font-size: 1.125rem; }
  .prose-custom blockquote {
    border-left: 3px solid var(--color-gold);
    padding-left: 1rem;
    margin: 1rem 0;
    color: var(--color-body);
    font-style: italic;
  }
  .prose-custom ul,
  .prose-custom ol {
    padding-left: 1.5rem;
    margin: 0.5rem 0;
  }
  .prose-custom li {
    margin-bottom: 0.25rem;
  }
  .prose-custom a {
    color: var(--color-link);
    text-decoration: underline;
  }
  .prose-custom a:hover {
    color: var(--color-link-deep);
  }
  .prose-custom strong { font-weight: 600; }
  .prose-custom em { font-style: italic; }
  .prose-custom u { text-decoration: underline; }
  .prose-custom p {
    margin-bottom: 0.5rem;
  }
  .prose-custom span[style] {
    display: inline;
  }
  .prose-custom table {
    display: block;
    width: max-content;
    max-width: 100%;
    overflow-x: auto;
    margin: 1rem 0;
    font-size: 0.9375rem;
  }
  .prose-custom th,
  .prose-custom td {
    border: 1px solid var(--color-hairline-strong, #b8a47a);
    padding: 8px 12px;
    vertical-align: top;
    text-align: left;
  }
  .prose-custom th {
    background: var(--color-canvas-soft);
    color: var(--color-ink);
    font-weight: 600;
  }
</style>

<script>
  function esc(s) {
    return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
  }

  function formatDate(iso) {
    try { return new Date(iso).toLocaleDateString(undefined, { year: "numeric", month: "short", day: "numeric" }); }
    catch { return iso; }
  }

  function sanitize(html) {
    return html
      .replace(/<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi, "")
      .replace(/\s+on\w+\s*=\s*(?:"[^"]*"|'[^']*'|[^\s>]+)/gi, "")
      .replace(/href\s*=\s*["']javascript:/gi, 'href="#"');
  }

  function renderContent(content) {
    return sanitize(content);
  }

  async function renderPost() {
    const container = document.getElementById("post-content");
    if (!container) return;
    try {
      const res = await fetch("/api/blog?slug=financial-freedom-necessity");
      if (!res.ok) throw new Error("Not found");
      const post = await res.json();
      if (post.status !== "published") throw new Error("Not found");
      document.title = `${esc(post.title)} — Global Wealth Portfolio Blog`;
      container.innerHTML = `
        <p class="text-caption-mono text-gold mb-xs">Blog</p>
        <h1 class="text-display-lg mb-md">${esc(post.title)}</h1>
        <p class="text-caption text-mute mb-lg">${formatDate(post.publishedAt ?? post.updatedAt)}${post.authorName ? ` · By ${esc(post.authorName)}` : ""}</p>
        <div class="prose-custom text-body-md text-body leading-relaxed space-y-md">${renderContent(post.content)}</div>
      `;
    } catch {
      container.innerHTML = `
        <p class="text-caption-mono text-gold mb-xs">Blog</p>
        <h1 class="text-display-lg mb-md">Post not found</h1>
        <p class="text-body-md text-body">The blog post you're looking for could not be found.</p>
        <a href="/blog" class="inline-flex items-center gap-xs text-body-sm text-link hover:underline mt-lg">&larr; Back to blog</a>
      `;
    }
  }

  renderPost();
</script>
